import { makeStyle } from '@hooks/themed-style.hook';

export const useSafetyPlanWizardStyles = makeStyle(() => ({
  root: {
    paddingTop: 15,
    height: '100%',
    position: 'relative',
    backgroundColor: 'transparent',
  },
}));
